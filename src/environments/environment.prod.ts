// src/environments/environment.prod.ts
export const environment = {
  production: true,
  AUDIT_ENCRYPTION_KEY: 'prod-encryption-key', // Use a secure key in production
  // Add other environment variables as needed
};